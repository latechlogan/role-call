# role-call
Role Call: A filterable reference of semantic HTML elements and their implicit/allowed ARIA roles. Built for developers prioritizing accessibility and reliable React Testing Library getByRole queries.
